---
title: "Advancing the Grammar of Graphics"
---

```{r, echo = FALSE, message = FALSE, warning = FALSE, warning = FALSE}
source(here::here("knitr-setup.R"))
source(here::here("libraries.R"))
```

# to do

- new platypus, maybe add a new creature for an exercise
HH: with the rinat package (on CRAN) we could let participants search for an animal of their choosing (rinat is based on data submitted to the iNaturalist app)
```
pelican6 <- get_inat_obs(query = "Pelican", year = 2025, month=6)
pelican12 <- get_inat_obs(query = "Pelican", year = 2024, month=12)
pelican <- rbind(pelican6, pelican12) 
pelican |> 
  ggplot(aes(x = longitude, y = latitude)) +  
  geom_path(aes(x = long, y = lat, group = group), data = map_data("world"), colour = "grey50") +
  geom_point(aes(colour = month(label=TRUE, ymd_hms(datetime)))) + 
  theme_bw() +
  scale_colour_manual("Month", values = c("purple", "orange"))
```

- talk about the ggverse, ggdist, https://exts.ggplot2.tidyverse.org
<!-- - HH: bug that I don't know how to fix: label in first leaflet map is not showing up at the right place (probably because of the pull-right) -->

## Example 2: Monotremes in Australia

Where can you find the strange platypus and echidna live in Australia?

![[Image Source](https://en.wikipedia.org/wiki/File:Platypus-sketch.jpg)](https://upload.wikimedia.org/wikipedia/commons/7/7e/Platypus-sketch.jpg){width="45%"}

![[Image Source](https://upload.wikimedia.org/wikipedia/commons/thumb/2/29/EchidnainCanberra.JPG/500px-EchidnainCanberra.JPG)](https://upload.wikimedia.org/wikipedia/commons/thumb/2/29/EchidnainCanberra.JPG/500px-EchidnainCanberra.JPG){width="45%"}

```{r not to run this code, eval=FALSE, echo = FALSE}
# Data extracted from ALA with this code
# Note that package ALA4Ris being replaced by package galah
# install.packages("galah")
library(galah)

galah_config(email = YOUR_EMAIL_HERE #"dicook@monash.edu",
             atlas = "Australia")

search_taxa(c("Ornithorhynchus", "Tachyglossidae"))

galah_call() |>
  galah_identify(c("Ornithorhynchus", "Tachyglossidae")) |>
  count() |> 
  collect()

monotremes <- galah_call() |>
  galah_identify(c("Ornithorhynchus", "Tachyglossidae")) |>
  galah_filter(year == 2024) |>   
  select(scientificName, eventDate, basisOfRecord, 
         decimalLongitude, decimalLatitude) |>
  atlas_occurrences()

monotremes <- monotremes |> 
  rename(
    longitude = decimalLongitude,
    latitude = decimalLatitude,
    datetime = eventDate,
    obs_type = basisOfRecord
  ) |>
  mutate(
    day = ymd(str_sub(datetime, 1, 10),
              tz="Australia/Sydney"), 
    hour = as.numeric(str_sub(datetime, 15, 16))) |>
    separate(scientificName, into = c("family", "species")) |>
  mutate(family = if_else(family == "TACHYGLOSSIDAE", "Tachyglossus", family)) |>
  mutate(common_name = if_else(
    family == "Tachyglossus", "echidna",
                              "platypus"))
save(monotremes, file="data/monotremes.rda")
```

```{r load the platypus obervation data, echo=TRUE}
load(here::here("data/platypus.rda"))
platydata <- platypus
ggplot(data=platydata) + geom_point(aes(x=longitude, y=latitude))
```

## Platypus in Australia

```{r Add some transparency to see density of locations, echo=TRUE, fig.width = 8, fig.height = 4, out.width = "80%"}
load(here::here("data/monotremes.rda"))
ggplot(data=monotremes) + 
  geom_point(aes(x = longitude, 
                 y = latitude, 
                 colour = family),
             alpha=0.5)
```

We can make the data look a bit more realistic with `coord_map`.

::: notes
If you are good at recognising the shape of Australia, you might realise that the sightings are all along the east coast and Tasmania. There is a strange location, that looks like someone saw one in Antarctica! We might need to filter this observation out later, because its extremely unlikely to have been found that far south.
:::

## Monotremes in Australia

::: columns
::: {.column width="25%"}
```{r making a map projection, echo=F, fig.width = 3, fig.height = 6, out.width="100%"}
ggplot(data=monotremes) + 
  geom_point(aes(x=longitude, 
                 y=latitude, 
                 colour = common_name), 
             alpha=0.1) +
  scale_colour_brewer("", palette = "Dark2") +
  coord_map()
```
:::
::: {.column width="75%"}
```{r making a map projection, echo=T, eval = F,fig.show=F, fig.height=10, out.width="100%"}
```

- Adding a map layer would help!
- Install the `ggmap` R package to use google maps as a background
:::
:::

## Monotremes in Australia

```{r you need a developer API to run this code, echo=FALSE, eval=FALSE}
library(ggmap)
library(osmdata)
oz_bbox <- c(112.9, # min long
              -45, # min lat
              159, # max long
              -10) # max lat
oz_map <- get_map(location = oz_bbox, source = "osm") 
save(oz, file="data/oz.rda")
```

```{r load the saved map data, echo=TRUE}
load(here::here("data/oz.rda"))
ggmap(oz) + 
  geom_point(data=monotremes, 
             aes(x=longitude, 
                 y=latitude, 
                 colour=common_name), 
              alpha=0.1) +
  scale_colour_brewer("", palette = "Dark2")
```

## Leaflet Monotreme Maps

```{r leaflet map, echo=TRUE, fig.width = 8, fig.height = 4}
#| output-location: "column"
library(leaflet)
monotremes |>
  filter(family == "Ornithorhynchus") |>
  filter(!is.na(latitude), 
         !is.na(longitude)) |>
  leaflet() |>
  addTiles() |>
  addCircleMarkers(
    radius=1, 
    opacity = 0.5, 
    color = "orange", 
    label = ~day,
    lat = ~latitude, lng = ~longitude) 
```

## Temporal trend

Plot sightings over time (`eventDate`). 
`eventDate` was converted into two variables during pre-processing: `day` which is a date variable, and `hour`.

```{r eval=FALSE}
SOME STUFF HERE |>
  mutate(
    day = ymd(str_sub(datetime, 1, 10),
              tz="Australia/Sydney"), 
    hour = as.numeric(str_sub(datetime, 15, 16)))
```

## Temporal trend
::: notes
then we can simply plot occurrence over time.
:::

```{r show sightings over time, echo=TRUE, out.width="60%"}
monotremes |>
  group_by(day, common_name) |>
  summarise(n = n()) |>
  ungroup() |>
  ggplot(aes(x=day, y=n)) +
    geom_point() +
    facet_wrap(~common_name)
```


There are some records dating back before 1800. There were only records from 1770! And these first records are in the database!

##  Temporal trend

There are a LOT more sightings in more recent years than in the 18th and 19th century, but this database has an amazing history!

```{r show jittered sightings over time, echo=TRUE, fig.height = 2, fig.width = 8, out.width="100%"}
ggplot(data=platydata) +
  geom_jitter(aes(x=eventDate, y=1), alpha = .2)
```


## Temporal trend

Let's focus on records since 1900, and count the sightings/year.

::: columns
::: column
```{r focus on records since 1900 and count the number for each year, echo=F, out.width="100%"}
platydata1900 <- platydata %>% 
  filter(year>1900) %>%
  count(year) 
ggplot(data=platydata1900) +
  geom_point(aes(x=year, y=n))
```
:::
::: column
```{r focus on records since 1900 and count the number for each year, echo=TRUE, eval=F}
```
:::
:::

```{r solution code for filtering on location and time, eval=FALSE}
# Check odd cases
platydata %>% filter(latitude < (-50)) 
# These just have the lat/long wrong
platydata %>% filter(eventDate < ymd("1850-01-01")) 
```

## Temporal trend

Add a trend line.


::: columns
::: column

```{r add a trend line, echo=TRUE,out.width="100%"}
monotremes |>
  group_by(day, common_name) |>
  summarise(n = n()) |>
  ungroup() |>
  ggplot(aes(x=day, y=n)) +
    geom_point() +
    geom_smooth(se=F) +
    facet_wrap(~common_name) 
```
:::
::: column
```{r add a trend line, echo=TRUE, eval=F}
```
:::
:::

## Interactivity!
```{r make it interactive to investigate some observations, echo=TRUE, fig.width = 8, fig.height = 4, out.width = "100%"}
library(plotly)
ggplotly()
```

## Your turn {.inverse .middle}

`r countdown::countdown(10)`

Make sure you can make all the plots shown in this session, and then tackle the next set of tasks: 

- Change the dotplot into a density plot, to focus on the locations of frequent sightings.
- Try facetting by creatures
- Do you learn anything different than from the scatterplot?


```{r solution code to density plot on map, eval=FALSE, echo=FALSE}
ggmap(oz) + 
  geom_density2d(data=monotremes, 
                 aes(x=longitude, y=latitude), 
              colour="orange") +
  facet_wrap(~common_name, ncol=2)
```

::: notes
Previously it looked like echidnas are found all over Australia. The density plot suggests they are found in the south-east similar to platypus. This possibly reveals a limitation of this data, that to be an occurrence requires a person to be present. There are a lot more people in Australia's southeast than the rest of the country, so it is possibly just showing where people are!
:::


## Your turn {.inverse .middle}

Have a go at making a side-by-side boxplot of the occurrences over month. How does this change the focus for the temporal trend. 

```{r solution code to boxplot task, eval=FALSE, echo=FALSE}
monotremes |>
  group_by(day, common_name) |>
  summarise(n = n(), .groups = "drop") |>
  mutate(month = month(day, label = TRUE, abbr = TRUE)) |>
  ggplot(aes(x=month, y=n)) +
    geom_boxplot() +
    facet_wrap(~common_name, ncol=2) 
```
::: notes
The focus now is median, and IQR over the year.
:::

## Your turn {.inverse .middle}

There is something quite wrong in the previous plots. Can you guess what it is?

::: notes
Days without any observations are missing from these plots. This is something that would need to be created in the pre-processing.

What should it be?
  
If no observations happened on any day, it would be reasonable to use 0 for these days. So we would need to complete the data to have a row for each day in the year, and fill the `n` column with 0. 
:::

## Resources {.smaller}

- [RStudio cheatsheets](https://www.rstudio.com/resources/cheatsheets/)
- [ggplot2: Elegant Graphics for Data Analysis](https://ggplot2-book.org), [web site](https://ggplot2.tidyverse.org)
- [R Graphics Cookbook, Winston Chang](http://www.cookbook-r.com/Graphs/)
- [Data Visualization, Kieran Healy](https://socviz.co)
- [Data Visualization with R, Rob Kabacoff](https://rkabacoff.github.io/datavis/index.html)
- [Fundamentals of Data Visualization, Claus O. Wilke](https://serialmentor.com/dataviz/)
- [Leaflet in R](https://rstudio.github.io/leaflet/)

::: bottom
<span style="display:inline-block;"><a rel="license" href="http://creativecommons.org/licenses/by-nc-sa/4.0/"><img alt="Creative Commons License" style="border-width:0" src="https://i.creativecommons.org/l/by-nc-sa/4.0/88x31.png" /></a></span><span style="display:inline-block;"> This work is licensed under a <a rel="license" href="http://creativecommons.org/licenses/by-nc-sa/4.0/">Creative Commons Attribution-NonCommercial-ShareAlike 4.0 International License</a>.</span>
:::
